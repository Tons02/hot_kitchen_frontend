export const STORAGE_KEYS = {
  session: 'hot-kitchen.session',
  theme: 'hot-kitchen.theme',
  /** The store a customer last chose to order from. */
  selectedStore: 'hot-kitchen.selected-store',
} as const
